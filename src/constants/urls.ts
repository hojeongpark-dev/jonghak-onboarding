export enum URL_LABELS {
  TIP = "Tip",
  EVENT = "Event",
  HOME = "Creatrip",
}

export enum URLS {
  LOGIN = "/login",
  TRIP_EVENT = "/tripevent",
  TRIP_EVENT_DETAIL = "/tripevent/:code",
  TIP = "/tip",
  TIP_DETAIL = "/tip/:code",
  HOME = "/",
}
